/**
 * Placeholder for the dashboard. The board UI lands in the next phase and the
 * data behind it in the one after; this exists so routing is exercised by real
 * tests from the first commit rather than being wired up on trust.
 */
export function BoardPage() {
  return (
    <main className="p-8">
      <h1 className="text-body-l font-semibold">Dashboard</h1>
    </main>
  )
}
