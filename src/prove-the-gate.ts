// Throwaway. Proves the required status check actually gates the merge button.
// `any` is a lint *error* in this repo, so `npm run gate` fails at the lint step.
export const brokenOnPurpose: any = 1
